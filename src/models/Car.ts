export default class Car {
    brand: string;
    model: string;
    plates: string;

    constructor(brand: string, model: string, plates: string) {
        this.brand = brand;
        this.model = model;
        this.plates = plates;
    }

}
